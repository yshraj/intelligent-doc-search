# LiveDocAI frontend – copy to .env and fill
# Only VITE_* vars are exposed to the browser (safe: anon key is public by design)

VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=
VITE_API_URL=http://localhost:8000
